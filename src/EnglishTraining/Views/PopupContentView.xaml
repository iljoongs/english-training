<UserControl x:Class="EnglishTraining.Views.PopupContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:EnglishTraining.Converters"
             MaxWidth="320">
    <UserControl.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibility" />
    </UserControl.Resources>
    <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4" Padding="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.25" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" TextWrapping="Wrap" />

            <StackPanel Visibility="{Binding Interpretation, Converter={StaticResource NullToVisibility}}" Margin="0,0,0,8">
                <TextBlock Text="해석" FontWeight="SemiBold" Foreground="#2563EB" />
                <TextBlock Text="{Binding Interpretation.Ko}" TextWrapping="Wrap" />
            </StackPanel>

            <StackPanel Visibility="{Binding Writing, Converter={StaticResource NullToVisibility}}" Margin="0,0,0,8">
                <TextBlock Text="영작" FontWeight="SemiBold" Foreground="#2563EB" />
                <TextBlock Text="{Binding Writing.Description}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Writing.Example}" TextWrapping="Wrap" FontStyle="Italic" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Visibility="{Binding Expression, Converter={StaticResource NullToVisibility}}">
                <TextBlock Text="표현" FontWeight="SemiBold" Foreground="#2563EB" />
                <TextBlock Text="{Binding Expression.Meaning}" TextWrapping="Wrap" />
                <TextBlock Text="{Binding Expression.Usage}" TextWrapping="Wrap" Margin="0,2,0,0" />
                <TextBlock Text="{Binding Expression.Example}" TextWrapping="Wrap" FontStyle="Italic" Margin="0,4,0,0" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
